<%= form_for @book do |f| %>
  <p>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title, :autofocus => true %>
  </p>

  <p>Author: <%= @book.author.name %></p>
  <%# hidden_field_tag 'book.', @book.author_id %>

  <%= f.submit %>

<% end %>