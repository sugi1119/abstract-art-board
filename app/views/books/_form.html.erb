<%= form_for @book do |f| %>
  <p>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title, :autofocus => true %>
  </p>

  <%= hidden_field_tag 'book[author_id]', @book.author_id %>

  <%= f.submit %>

<% end %>